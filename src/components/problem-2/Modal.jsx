import React from 'react';

const Modal = ({handleClose, contacts, usa, show, setShow}) => {
    return (
        <div>
            <h2>will show modal data</h2>
        </div>
    );
};

export default Modal;